@model EReaderApp.Models.PublicationLike

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>PublicationLike</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="FKIdUser" class="control-label"></label>
                <select asp-for="FKIdUser" class ="form-control" asp-items="ViewBag.FKIdUser"></select>
            </div>
            <div class="form-group">
                <label asp-for="FKIdPublication" class="control-label"></label>
                <select asp-for="FKIdPublication" class ="form-control" asp-items="ViewBag.FKIdPublication"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
